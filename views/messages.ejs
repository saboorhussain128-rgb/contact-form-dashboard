<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Messages</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="admin">

    <h1>📩 Contact Messages</h1>

    <div class="top-links">
        <a href="/">⬅ Back To Contact Form</a>
        |
        <a href="/logout">🚪 Logout</a>
    </div>

    <!-- 🔥 TOAST NOTIFICATIONS -->
    <%- include('partials/toast') %>

    <!-- SEARCH -->
    <div style="text-align:center; margin-bottom:20px;">
        <form action="/messages" method="GET">

            <input
                type="text"
                name="search"
                placeholder="Search messages..."
                value="<%= search || '' %>"
            >

            <button type="submit">Search</button>
        </form>
    </div>

    <!-- TOTAL -->
    <div style="text-align:center; margin-bottom:20px;">
        <h3>Total Messages: <%= totalMessages %></h3>
    </div>

    <% if (!messages || messages.length === 0) { %>

        <p class="no-data">No messages found.</p>

    <% } else { %>

        <div class="cards">

            <% messages.forEach(msg => { %>

                <div class="card">

                    <div style="display:flex; justify-content:space-between; margin-bottom:10px;">

                        <a href="/messages/edit/<%= msg._id %>"
                           style="background:#3b82f6; color:white; padding:6px 10px; border-radius:6px; text-decoration:none;">
                            Edit
                        </a>

                        <form action="/messages/delete/<%= msg._id %>" method="POST">
                            <button type="submit" class="delete-btn"
                                onclick="return confirm('Are you sure you want to delete this message?')">
                                Delete
                            </button>
                        </form>

                    </div>

                    <h3><%= msg.firstName %> <%= msg.lastName %></h3>

                    <p>
                        <strong>Email:</strong>
                        <a class="email-link" href="mailto:<%= msg.email %>">
                            <%= msg.email %>
                        </a>
                    </p>

                    <% if (msg.phone) { %>
                        <p>
                            <strong>Phone:</strong>
                            <a class="phone-link" href="tel:<%= msg.phone %>">
                                <%= msg.phone %>
                            </a>
                        </p>
                    <% } %>

                    <p><%= msg.message %></p>

                    <small>
                        📅 <%= new Date(msg.createdAt).toLocaleString() %>
                    </small>

                </div>

            <% }) %>

        </div>

        <div style="text-align:center; margin-top:30px;">

            <% for(let i = 1; i <= totalPages; i++) { %>

                <a href="/messages?page=<%= i %>&search=<%= search || '' %>"
                   style="margin:5px; padding:8px 14px; background:#a855f7; color:white; border-radius:6px; text-decoration:none;">
                    <%= i %>
                </a>

            <% } %>

        </div>

    <% } %>

</body>
</html>